# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables / secrets
.env
.env.*
!.env.example

# מפתח ה-service account של Firebase — סוד. גישה מלאה לפרויקט, עוקף כל כלל אבטחה.
firebase-service-account.json
*-service-account.json

# ייצוא המסד. מכיל שמות וטלפונים של מאות מוזמנים אמיתיים — לא נכנס ל-git לעולם.
migration/

# PostgreSQL זמני של npm run test:db
.pgdata

# קבצי עבודה זמניים של בדיקות ידניות (קוקיז, פלטי סקריפטים, קבצי דוגמה)
tmp/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
